# Schema Conversion Report

Generated at: `2026-05-22T07:22:17.041497+00:00`

| Item | Value |
|---|---|
| Selected source profile | PostgreSQL |
| Azure target | Azure Database for PostgreSQL Flexible Server |
| Conversion mode | Native PostgreSQL-compatible migration |
| SSMA required | No |

## Schema Object Summary

| Profile | Tables | Indexes | FK References | Identity Columns | Unique Constraints |
|---|---:|---:|---:|---:|---:|
| h2 | 7 | 6 | 5 | 6 | 0 |
| mysql | 7 | 0 | 5 | 6 | 1 |
| postgres | 7 | 7 | 5 | 6 | 1 |

## Findings And Remediation

| ID | Severity | Source | Finding | Remediation |
|---|---|---|---|---|
| SC-001 | info | postgres | PostgreSQL schema is already available and compatible with Azure Database for PostgreSQL Flexible Server. | Use native PostgreSQL migration with pg_dump and pg_restore; no SSMA conversion is required. |
| SC-002 | medium | h2 | H2 uses VARCHAR_IGNORECASE for owners.last_name. | Target PostgreSQL uses TEXT. If case-insensitive search must be preserved at the database layer, add citext or a lower(last_name) functional index. |
| SC-003 | low | h2 | H2 drop-table syntax and embedded identity behavior are not target deployment patterns. | Use PostgreSQL DDL with CREATE TABLE IF NOT EXISTS and GENERATED BY DEFAULT AS IDENTITY. |
| SC-004 | medium | mysql | MySQL schema uses AUTO_INCREMENT, UNSIGNED integer display widths and engine=InnoDB. | PostgreSQL target maps identity columns to GENERATED BY DEFAULT AS IDENTITY, removes UNSIGNED/display widths and omits storage-engine syntax. |
| SC-005 | info | all | PetClinic schema has seven application tables: vets, specialties, vet_specialties, types, owners, pets and visits. | Validate row counts, checksums, FK relationships and identity sequence values after restore. |

## Post-Migration Validation Required

- Row counts per table must match.
- Checksums/hashes for critical tables must match.
- Foreign-key spot checks must pass for pets, visits and vet specialties.
- Identity sequence next values must be greater than the max table ID.
- Application smoke queries must pass after connection string cutover.
